{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy; npx tsx prisma/seed.ts; npx tsx src/prod.ts",
    "restartPolicyType": "ON_FAILURE"
  }
}
